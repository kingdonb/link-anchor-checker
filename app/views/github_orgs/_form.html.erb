<%= form_with(model: github_org) do |form| %>
  <% if github_org.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(github_org.errors.count, "error") %> prohibited this github_org from being saved:</h2>

      <ul>
        <% github_org.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
